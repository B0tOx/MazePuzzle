public class wallContainer {
  PVector pt;
  int type;
  float texDisX;

  wallContainer(PVector pt, int type, float texDisX) {
    this.pt = pt;
    this.type = type;
    this.texDisX = texDisX;
  }
}
